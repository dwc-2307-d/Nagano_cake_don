<table class="table elegant-table">
  <thead>
    <tr><th class="pl-3 pr-10 genre-font ">ジャンル検索</th></tr>
  </thead>
  <tbody>
    <% @genres.each do |genre| %>
      <tr><td><%= link_to genre.name, genre_path(genre), class:"link elegant-link" %></td></tr>
    <% end %>
  </tbody>
</table>
